---
# Directories containing cluster type definitions
#type_paths:
#  -
#  -

# Path to Flight Hunter executable
#hunter_command:

# Enable the usage of nodes found by hunter (EXPERIMENTAL)
# Requires `hunter_command` to be set
#use_hunter:

# Secret to use when encrypting strings
#shared_secret_path:

# The cluster type to be configured
#cluster_type:

# Path to which the log file is saved
#log_dir:

# Enable automatic node removal from hunter list after being applied
#remove_hunter_entry:

# remove the node from the cluster on instance shutdown
#remove_on_shutdown